econ_ge:
  target: dev
  outputs:
    dev:
      type: duckdb
      # Same file as src.config.settings paths.duckdb_path — exported by the
      # root .envrc so Python and dbt cannot drift to different warehouses.
      path: "{{ env_var('DUCKDB_PATH') }}"
      schema: main
      threads: 4
